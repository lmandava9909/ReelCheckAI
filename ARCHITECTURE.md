# ReelCheck AI architecture

Instagram URL → extraction → caption/media/visible text → transcription → transcript guardrails → statement classification → claims → semantic chunks → independent evidence → evidence relationships → deterministic verdict guardrails → ReelCheck Brief.

Vector or hybrid retrieval is intended for semantic context and provenance. Similarity is never treated as independent evidence.
